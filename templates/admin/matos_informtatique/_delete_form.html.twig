<form method="post" action="{{ path('app_admin_matos_informtatique_delete', {'id': matos_informatique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matos_informatique.id) }}">
    <button class="btn">Delete</button>
</form>
