{% extends 'base.html.twig' %}

{% block title %}MatosInformatique{% endblock %}

{% block body %}
    <h1>MatosInformatique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matos_informatique.id }}</td>
            </tr>
            <tr>
                <th>Type_matos</th>
                <td>{{ matos_informatique.typeMatos }}</td>
            </tr>
            <tr>
                <th>Marque_matos</th>
                <td>{{ matos_informatique.marqueMatos }}</td>
            </tr>
            <tr>
                <th>Modele_matos</th>
                <td>{{ matos_informatique.modeleMatos }}</td>
            </tr>
            <tr>
                <th>Sn_matos</th>
                <td>{{ matos_informatique.snMatos }}</td>
            </tr>
            <tr>
                <th>Date_reception</th>
                <td>{{ matos_informatique.dateReception ? matos_informatique.dateReception|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Specification</th>
                <td>{{ matos_informatique.specification }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ matos_informatique.createdAt ? matos_informatique.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ matos_informatique.updatedAt ? matos_informatique.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_matos_informtatique_index') }}">back to list</a>

    <a href="{{ path('app_admin_matos_informtatique_edit', {'id': matos_informatique.id}) }}">edit</a>

    {{ include('admin/matos_informtatique/_delete_form.html.twig') }}
{% endblock %}
