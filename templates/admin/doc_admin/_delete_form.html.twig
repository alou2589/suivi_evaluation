<form method="post" action="{{ path('app_admin_doc_admin_delete', {'id': document_administratif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document_administratif.id) }}">
    <button class="btn">Delete</button>
</form>
