<form method="post" action="{{ path('app_admin_type_matos_delete', {'id': type_mato.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_mato.id) }}">
    <button class="btn">Delete</button>
</form>
