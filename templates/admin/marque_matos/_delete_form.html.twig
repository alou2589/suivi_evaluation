<form method="post" action="{{ path('app_admin_marque_matos_delete', {'id': marque_mato.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marque_mato.id) }}">
    <button class="btn">Delete</button>
</form>
