{% if maintenance.ficheMaintenance %}
    <iframe src="{{ path('app_admin_maintenance_show_fiche_maintenance', {id: maintenance.id}) }}" width="100%" height="600px"></iframe>
{% else %}
    <p>Aucune fiche de maintenance disponible</p>
{% endif %}
