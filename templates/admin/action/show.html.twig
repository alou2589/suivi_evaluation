{% extends 'base.html.twig' %}

{% block title %}Action{% endblock %}

{% block body %}
    <h1>Action</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ action.id }}</td>
            </tr>
            <tr>
                <th>Nom_action</th>
                <td>{{ action.nomAction }}</td>
            </tr>
            <tr>
                <th>Code_action</th>
                <td>{{ action.codeAction }}</td>
            </tr>
            <tr>
                <th>Cout_action</th>
                <td>{{ action.coutAction }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ action.createdAt ? action.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ action.updatedAt ? action.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_action_index') }}">back to list</a>

    <a href="{{ path('app_admin_action_edit', {'id': action.id}) }}">edit</a>

    {{ include('admin/action/_delete_form.html.twig') }}
{% endblock %}
