<form method="post" action="{{ path('app_admin_carte_professionnelle_delete', {'id': carte_professionnelle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte_professionnelle.id) }}">
    <button class="btn">Delete</button>
</form>
