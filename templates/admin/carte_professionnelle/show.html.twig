{% extends 'base.html.twig' %}

{% block title %}CarteProfessionnelle{% endblock %}

{% block body %}
    <h1>CarteProfessionnelle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carte_professionnelle.id }}</td>
            </tr>
            <tr>
                <th>Photo_agent</th>
                <td>{{ carte_professionnelle.photoAgent }}</td>
            </tr>
            <tr>
                <th>Date_delivrance</th>
                <td>{{ carte_professionnelle.dateDelivrance ? carte_professionnelle.dateDelivrance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_expiration</th>
                <td>{{ carte_professionnelle.dateExpiration ? carte_professionnelle.dateExpiration|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status_impression</th>
                <td>{{ carte_professionnelle.statusImpression }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ carte_professionnelle.createdAt ? carte_professionnelle.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ carte_professionnelle.updatedAt ? carte_professionnelle.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_carte_professionnelle_index') }}">back to list</a>

    <a href="{{ path('app_admin_carte_professionnelle_edit', {'id': carte_professionnelle.id}) }}">edit</a>

    {{ include('admin/carte_professionnelle/_delete_form.html.twig') }}
{% endblock %}
